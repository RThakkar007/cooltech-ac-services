[build]
  base    = "cooltech-static"
  command = "npm install -g pnpm && pnpm install --ignore-scripts && pnpm build"
  publish = "dist"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
